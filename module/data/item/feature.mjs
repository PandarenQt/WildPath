import WildPathBaseItem from "./base.mjs";

/**
 * A passive trait, talent, or feature. No additional schema beyond the base Item fields
 * (description + modifiers + active) is required for the groundwork phase.
 */
export default class WildPathFeature extends WildPathBaseItem {}
